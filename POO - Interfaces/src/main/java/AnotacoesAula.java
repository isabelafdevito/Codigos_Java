public class AnotacoesAula {

    /*

    classe abstrata:
    métodos abstratos só tem cabeçalho, e voce obrigatoriamente tem que criar o overwrite dele na filha
    - possui construtores
    - uma classe que não pode ser instanciada
    - usada para prover herança e polimorfismo
    - o java só permite herança simples (cada classe só pode ter uma mãe)

    Interfaces:
    - não possui construtores
    - uma classe pode ter mais de uma interface (herança multipla)
    - não pode ter atributos, só cabeçalhos de métodos (por isso os metodos são todos abstratos)
    - suporta apenas constantes
    - as classes que implementam uma interface obrigatoriamente tem que fazer overwrite dos métodos da interface
    - TODOS OS METODOS DE UMA INTERFACE SAO PUBLIC ABSTRACT por padrão

     */

}
